# 2026-06-15

## 完成事项
- 为 `Sources/DesktopPet/App/` 目录下全部 12 个 Swift 文件添加了中文注释（共 216 条注释/MARK），包括：
  - 文件级说明注释
  - 类型/协议/枚举的文档注释
  - 关键方法的参数和功能说明
  - 复杂逻辑的内联注释
  - switch case 的分组 MARK 注释
  - 依赖注入容器的分层注释
- 处理文件：ActionTriggerService, ActionsMenuBuilder, AppCommand, AppCoordinator, AppDelegate, BubbleCommander, DesktopPetLog, DesktopPetResources, MenuBarController, PetContextMenuBuilder, PetLibraryCommander, SettingsWindowController
- 修复了注释编辑引入的 2 个语法错误（menuState 和 handleQuietForOneHour 被意外吃掉声明行）
- 成功构建并启动了 MofuPaw（PID 32437），使用 `swift run --disable-sandbox MofuPaw`
- 实现了语言切换功能：
  - 新建 AppLanguage 枚举（zh-Hans/en）、LanguageManager、L10n 字符串集中管理
  - 菜单栏新增「语言」子菜单，可切换 简体中文/English
  - 设置页面新增「界面语言」Section，Picker 切换
  - 更新 AppCommand 新增 setAppLanguage case
  - 所有菜单栏文案和设置页 Section 标题改用 L10n 集中管理
- 补全设置页子视图的未跟随语言文字：
  - PetBubbleSettingsView（气泡频率标签）
  - CompanionshipSettingsView（关系提示、昵称、重置确认）
  - InteractiveBubbleSettingsView（智能气泡、活跃度、高级设置）
  - CustomPetPlaceholderView（计划提示）
  - PetLibraryView（导入按钮、空状态提示）
  - L10n 扩展至 200+ 条双语条目，覆盖 7 个功能域
- 补全 AI 设置页面本地化：
  - AISettingsView：AI 开关、服务商配置、性格、记忆管理、隐私弹窗全部接入 L10n
  - AIVisualSettingsView：视觉开关、图像服务商、日常用量、频率/时长/强度/一致性、手动生成、历史、启用须知全部接入 L10n
  - 修复 InteractiveBubbleSettingsView 测试文件中移除的静态属性引用
- 设置页面现在切换语言后，所有 Section 标题、标签、按钮、弹窗文案全部跟随
